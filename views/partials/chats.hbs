  <ul hx-swap-oob="outerHTML:#chats" id="chats" class="my-2 overflow-y-scroll">
    {{#each chats}}
      <li class="truncate cursor-pointer p-2 rounded hover:bg-gray-200 active:bg-gray-300{{#isEqual chatId=chatId currentChatId=../currentChatId}} bg-gray-300{{/isEqual}}">
        <form ws-send hx-disabled-elt="this">
            <input type="hidden" name="cmd" value="old_chat" />
            <input type="hidden" name="chatId" value="{{chatId}}" />
            <button type="submit" class="text-gray-600">
                {{!-- Trim the end and add an elipsis --}}
                {{message}}
            </button>
        </form>
      </li>
    {{/each}}
  </ul>
